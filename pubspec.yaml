name: album_colorare
description: Album da colorare per bambine
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_drawing_board: ^0.5.6
  image: ^4.1.3
  image_picker: ^1.0.7
  gal: ^2.3.0
  share_plus: ^9.0.0
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3
  http: ^1.2.1
  url_launcher: ^6.2.6
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
